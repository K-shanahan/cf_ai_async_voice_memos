{
  "name": "@project/backend",
  "version": "0.1.0",
  "description": "Cloudflare Workers backend for Voice Memo Task Manager",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler publish --dry-run",
    "deploy": "wrangler publish",
    "test": "vitest --run --exclude '**/*.load.test.ts'",
    "test:ui": "vitest --ui --exclude '**/*.load.test.ts'",
    "test:coverage": "vitest --coverage --run --exclude '**/*.load.test.ts'"
  },
  "dependencies": {
    "@project/shared": "workspace:*",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251014.0",
    "@jridgewell/sourcemap-codec": "^1.5.0",
    "@types/node": "^20.10.0",
    "@types/uuid": "^10.0.0",
    "@vitest/coverage-v8": "^2.1.5",
    "@vitest/ui": "^2.1.5",
    "typescript": "^5.3.3",
    "undici": "^6.19.0",
    "vitest": "^2.1.5",
    "wrangler": "^3.71.0"
  }
}
